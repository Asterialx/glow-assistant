/**
 * TEMP (closed beta): email OTP disabled until a custom Resend domain is ready.
 * Restore: set to `true`, then re-enable Confirm email in Supabase.
 * Checkpoint with full OTP UI: git commit 8bcde62
 */
export const AUTH_EMAIL_OTP_ENABLED = false;
